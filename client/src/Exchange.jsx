import React, { useState, useEffect } from 'react'

function Exchange() {
    //   const [loading, setLoading] = useState(true);
    // useEffect(() => {
    //     Promise(
    //         fetch('https://localhost:3001/live').then(() => {
    //             if (!r.ok) throw new Error('Exchange fetch failed');
    //     return r.json();
    //         })
    //     )
    // })
    return (
        <div>Exchange</div>
    )
}

export default Exchange